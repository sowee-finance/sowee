# Submission screenshots (16:9)

1920×1080, the ratio ETHGlobal asks for. Captured from what is running, not mocked up.

| File | What it shows | Source |
|---|---|---|
| `1-landing.png` | `sowee.site` — a live listing drawn as a bond card, read from `InvoiceMarket` | `apps/landing` |
| `2-marketplace.png` | the market: top yields, maturing soon, newly issued, and every listing | `app.sowee.site` |
| `3-bond.png` | one bond — implied APY, the accretion to par, funding progress, secondary asks | `app.sowee.site` |
| `4-audit.png` | the same bond's facts, its ATS security token (ISIN, Reg S) and its HCS audit trail | `app.sowee.site` |
| `5-issue.png` | issuing: the invoice is hashed in the browser and never uploaded | `app.sowee.site` |

The cover image for the submission is `../cover.png` (1920×1080, with a 640×360 copy beside it):
built for the gallery rather than cropped from a screenshot, because a full page of UI is
unreadable at that size. Its figures are the live `sINV010` listing.

The square 1620×1620 cards next door (`docs/showcase/*.png`) are for the gallery field, which
asks for a different ratio.
