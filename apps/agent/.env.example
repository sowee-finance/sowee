SOWEE_API_URL=http://localhost:8080
# The agent's own Hedera testnet account: needs USDC (0.0.429274) associated and funded.
# It pays 0.01 USDC per call; the facilitator pays the network fee.
HEDERA_ACCOUNT_ID=0.0.
HEDERA_PRIVATE_KEY=
